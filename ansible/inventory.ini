[target_vm]
# Local placeholder for syntax checks. The GitHub Actions workflow overwrites this file
# with the live VM IP and SSH private key path before running the deployment.
localhost ansible_connection=local ansible_user=ubuntu
